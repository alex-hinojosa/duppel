# Duppel status — September 2026 (Sprint 0)

Sprint 0 is **docs and license hygiene only**. No production behavior changes in this sprint.

## Phase B report is stale

`PHASE-B-REPORT.md` (dated 2026-05-24, authored against v0.1.0-era behavior) still documents HTTP–JS split-brain and related findings. Treat that report as historical measurement, not current proof.

Before claiming split-brain is closed (or quoting Phase B gates as green):

```bash
npm run test:phase-b
```

Re-run the harness and refresh the report from fresh `test-results/phase-b-measurements.jsonl` output. Do not assert coherence from the May 2026 write-up alone.

## A1 — Client Hints HTTP (still open)

Client Hints on the HTTP wire remain **stripped via declarativeNetRequest**, not set to persona-aligned values.

- **Current:** DNR strips Client Hints headers (strip path).
- **Not done:** persona `SET` for Client Hints over HTTP (A1 follow-up).
- Sprint 0 does **not** implement Client Hints DNR SET.

Keep A1 open until a dedicated follow-up lands and is gated by Proof.

## Sprint 0 scope

| In scope | Out of scope |
|----------|--------------|
| `package.json` / lockfile license → `GPL-3.0-only` | Retagging `v0.1.1` |
| README architecture / naming corrections | Client Hints DNR SET (A1) |
| `manifest.json` description honesty | Production fingerprint / DNR / bootstrap changes |
| This status note | Claiming Phase B gates from stale report |

## Proof ask

After this docs/license PR merges, Proof should gate on a fresh `npm run test:phase-b` (and any other release gates) before treating coherence or A1 as closed.
